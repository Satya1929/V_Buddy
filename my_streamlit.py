# fix this , multiple uploads , ans answer only after a single upload
# then do multipage
    #developer page to see uploaded database files
    #user dashboard = main page + conversational UI
                    #answer it from pdf only and not from internet , also if taken from internet mention that
# to try different UI
# then deploy it (streamlit free deployement + amazon aws deployment )